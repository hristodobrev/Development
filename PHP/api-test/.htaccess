<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_URI} !^(/[a-zA-Z0-9_-]+)?/public/.*$
  RewriteCond %{REQUEST_URI} !^(/[a-zA-Z0-9_-]+)?/uploads/.*$
  RewriteRule ^ index.php
</IfModule>